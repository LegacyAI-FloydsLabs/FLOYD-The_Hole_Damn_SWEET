# Optional initial workspace. The --workspace CLI flag takes precedence.
CURSEM_WORKSPACE_ROOT=/absolute/path/to/project

# Frame supplies these values from CURSEM's owner-only application profile.
# Never place a provider credential in this file.
FLOYD_VAULT_APP_PROFILE=/path/to/runtime/secrets/proxy-app-profiles/cursem.json
